# Known issues

Documented gotchas with diagnosed cause. Short files (≤150 lines), named after the symptom.

Auto-loaded by the `pr-review` skill as a **false-positive shield** — patterns that look wrong but are intentional (HMR quirks, auth edge cases, infra workarounds) won't be re-flagged.

Created via `/preserve` — see `.claude/commands/preserve.md` for the known-issue template.
